# Production Desired State

Production is generated by the ApplicationSet but is not automatically synchronized.

Production accepts only the digest already promoted through QA. The intended process is a reviewed pull request followed by an explicit Argo CD synchronization.
