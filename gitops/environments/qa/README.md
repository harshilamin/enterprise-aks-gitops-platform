# QA Desired State

QA is generated by the ApplicationSet but is not automatically synchronized.

Promote the exact Development image digest into this directory through a pull request, review the rendered change, merge it, and then manually synchronize `sample-api-qa`.
